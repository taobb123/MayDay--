# 选型记录 009 · 支付订单骨架

## 1. 现状摸底

- 仅有 `mock_upgrade_membership`，无订单表、无 Webhook。

## 2. 选型

| 项 | 选择 | 原因 |
|----|------|------|
| 默认 | `PAYMENT_PROVIDER=mock` | 本地可验收 |
| 真实 | Stripe Checkout Session（可选依赖） | 与计划文档一致；国内支付另开刀 |
| 履约 | 订单 `mark_paid` → `mock_upgrade_membership` | 复用会员开通 |
| 密钥 | django settings ← env / dotenv | 不入库 |

## 3. 本刀切片

1. `MembershipOrder` + migration + admin  
2. `payments.py`：checkout / fulfill / stripe webhook handler  
3. API + 会员页 CTA 更新  

## 4. 默认假设

- 金额默认 990 分（¥9.90）/ 30 天，可用环境变量覆盖。  
- 微信/支付宝留待后续纪要。

## 5. 本刀交付回写

- 模型 `MembershipOrder` + migration `0008`；`payments.py`。
- API：`/api/payments/checkout/`、`/api/payments/webhook/stripe/`；成功页 `/membership/success/`。
- 配置：`.env.example`；`PAYMENT_PROVIDER` / Stripe 环境变量。
- 会员页 CTA「开通会员」；默认 mock 履约。
