# 选型记录 006 · 会员骨架

## 1. 现状摸底

- 仅有 Django User + Playlist；无会员字段/支付。
- 闭环 P2「会员商业化」尚未开工。

## 2. 选型

| 项 | 选择 | 原因 |
|----|------|------|
| 模型 | `MembershipProfile` OneToOne User | 不改 auth.User 表结构 |
| 支付 | **Mock 升级 API** | 骨架验证权益，不接 Stripe |
| 门控 | 免费歌单上限 **5** | 可验收、改动面小 |
| 演示时长 | Mock 开通 **30 天** | 可感知到期字段 |

## 3. 本刀切片

1. 模型 + migration；注册时自动建 profile（默认 free）。  
2. `/membership/` 页 + 侧栏；`/api/membership/status|upgrade/`。  
3. `create_playlist_api` / ViewSet 创建门控。

## 4. 默认假设

- 「高清/推荐/无广告」仅文案预告，本刀不实现。  
- Admin 可手动改 plan（已注册 `MembershipProfileAdmin`）。

## 5. 本刀交付回写

- 模型 `MembershipProfile` + migration `0006`；`membership.py` 状态/升级/门控。
- 页 `/membership/`；API `status` / `upgrade`（mock 30 天）。
- 免费歌单上限 5；侧栏「会员」入口。
