# 选型记录 008 · 底栏收藏心形

## 1. 现状摸底

- 007 已有 `toggleFavorite` / `favoritedSongIds`；底栏无心形。
- `updatePlayerUI` / `playSong` / `restorePlayerState` 可挂钩同步。

## 2. 选型

| 项 | 选择 |
|----|------|
| 位置 | 曲名旁（`record-info` 右侧）或主控前；本刀放在 **上一首之前**，易发现 |
| API | 复用 toggle + 启动时拉 `ids`（已登录） |
| 同步 | `syncPlayerFavoriteButton()` + toggle 成功后刷新底栏 |

## 3. 本刀切片

1. 底栏按钮 DOM + 样式  
2. `syncPlayerFavoriteButton` / 登录态预拉 ids  
3. 挂钩 `updatePlayerUI`、`playSong`、restore  

## 4. 默认假设

- 真实支付仍后置。

## 5. 本刀交付回写

- 底栏 `#playerFavoriteBtn`；`togglePlayerFavorite` / `syncPlayerFavoriteButton` / `ensureFavoriteIdsLoaded`。
- `updatePlayerUI` 与 toggle 成功后同步列表+底栏。
