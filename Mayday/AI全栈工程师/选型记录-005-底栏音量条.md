# 选型记录 005 · 底栏音量条

## 1. 现状摸底

- 底栏已有进度、上一首/下一首、队列、模式；**无 volume UI**。
- `savePlayerState` / `restorePlayerState` 未持久化 `volume`。

## 2. 选型

| 项 | 选择 | 原因 |
|----|------|------|
| 实现位置 | 继续 `base.html` 内联 | 与播放器同生命周期 |
| 持久化 | `playerState.volume` + 独立 `playerVolume` 键兜底 | 关播放器后仍记住偏好 |
| 静音 | 图标切换，记住 `volumeBeforeMute` | Spotify/Apple 常见交互 |

## 3. 本刀切片

1. CSS + 底栏 volume 控件。  
2. `setVolume` / `toggleMute` + 初始化。  
3. 写入/恢复 localStorage。

## 4. 默认假设

- 默认音量 **0.8**。  
- 会员骨架留待后续刀。

## 5. 本刀交付回写

- `base.html`：底栏音量滑条 + 静音；`setVolume` / `toggleMute` / `initPlayerVolume`。
- 持久化：`localStorage.playerVolume` + `playerState.volume`。
